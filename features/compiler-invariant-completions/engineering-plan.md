# Compiler Invariant Completions — Engineering Plan

**Brief:** [`./brief.md`](./brief.md) · **Decisions:** [`./decisions.md`](./decisions.md)

**Created:** 2026-05-12
**Last updated:** 2026-05-14

## Brief mapping

### Goals

| Brief Goal | Delivered by chunks |
|---|---|
| Each cross-IR defect surfaces under exactly one rule | `xref-context-check-introduction` (CRUD-surface uniform pattern), `xref-narrowing-and-rename` (V020 narrowing + per-target X003 + vocabulary rename — delivers the dedupe property) |
| Every IR shape variant is corpus-evidenced or explicit escape-hatch | `audit-marker` (marker trait), `audit-marker-retrofit` (apply markers to existing reachable internal-state enums), `audit-harness` (CI test + walks + contingent variant deletion) |
| Generated build artifacts are byte-deterministic, asserted by CI | `harvester-pure-core` (extract pure core), `harvester-determinism-audit` (CI test + SHA ledger — delivers the byte-determinism property) |
| No IR variant payload bypasses the IR shape with raw bytes | `nonsummon-typed-ir` (atomic IR retype — delivers the no-raw-passthrough invariant), `nonsummon-typed-authoring` (T34 strict-authoring complement) |
| Derived-structural regenerators are complete and corpus-matched | `complete-regenerators` |
| Every authorable IR type has a construction primitive requiring identity | `authorable-new-constructors` |
| The itempool extractor produces typed summons; cross-IR findings carry typed cross-bucket fields | `typed-summon-extractor` |
| SPEC.md and authority-doc vocabulary match the shipped IR | `authority-doc-vocab-alignment` |
| `ReplicaItem` summon-name vocabulary is generic, not Pokemon-specific | `summon-name-rename` |

### User-facing changes

| Brief change | Delivered by chunks | Verified by |
|---|---|---|
| A name shared across the hero, replica-item, and monster categories produces one diagnostic per defect, not two | `xref-context-check-introduction`, `xref-narrowing-and-rename` | `xref-narrowing-and-rename` |
| New audit fails if any in-scope IR shape is neither corpus-evidenced nor on the closed escape-hatch registry | `audit-marker`, `audit-marker-retrofit`, `audit-harness` | `audit-harness` |
| New audit asserts generated source files are byte-identical across repeated harvester invocations, surfacing process-distinguishable input as byte divergence | `harvester-pure-core`, `harvester-determinism-audit` | `harvester-determinism-audit` |
| Audit's first run identifies unevidenced in-scope IR shapes; deletion plus authority-doc bundle lands inside the audit chunk | `audit-harness` | `audit-harness` |
| `ItempoolItem::NonSummon` retypes from `content: String` to a fully typed recursive `NonSummonEntry` sum; novel shapes demote to paired `Finding` rather than raw round-trip | `nonsummon-typed-ir`, `nonsummon-typed-authoring` | `nonsummon-typed-ir` (delivers the no-raw-passthrough invariant); `nonsummon-typed-authoring` (delivers T34 strict-authoring complement) |
| `PoolReplacement` regenerator + corrected `generate_char_selection` body ship; `_ => {}` deferred sink in `regenerate_derived_kinds` retires | `complete-regenerators` | `complete-regenerators` |
| `HeroBlock`, `ReplicaItem`, `AbilityData`, `TriggerHpDef` ship `::new(identity)` constructors with non-defaulted required fields; no `Default` derive on any | `authorable-new-constructors` | `authorable-new-constructors` |
| `extract_from_itempool` produces typed `ItempoolItem::Summon` entries plus populated `ReplicaItem`; `ReplicaItem` schema widens to recursive nested-egg sum; `Finding` carries typed `buckets` + `includes_boss` fields | `typed-summon-extractor` | `typed-summon-extractor` |
| SPEC.md no longer carries retired ReplicaItem-kind names as live IR identifiers; §10 glossary entry's heading strips them while role description survives | `authority-doc-vocab-alignment` | `authority-doc-vocab-alignment` |
| `ReplicaItem.target_name` renames to `summon_name` atomically across every callsite; no parallel `target_name` survives | `summon-name-rename` | `summon-name-rename` |

### Non-goals enforcement

| Non-goal | Enforcement |
|---|---|
| No re-prosecution of already-shipped platform invariants (bounded carve-outs for `PoolReplacement`/`generate_char_selection` and four `::new` constructors) | No chunk touches shipped platform mechanism beyond the two enumerated carve-outs; `complete-regenerators` authors missing/malformed regenerators paralleling shipped ones; `authorable-new-constructors` adds constructors only on the four enumerated types |
| No new author-ergonomics surface (bounded carve-outs for `compiler/src/authoring/non_summon_entry.rs` and four `::new` constructors) | `compiler/src/authoring/` is treated as terminal for this feature with the two enumerated bounded carve-outs; `nonsummon-typed-authoring` is the single authoring-builders sibling chunk authorizing the `non_summon_entry.rs` carve-out per the no-new-author-ergonomics Non-goal and decisions.md 2026-05-11 amendment |
| No parser or emitter behavior changes beyond what the four bounded carve-outs require | Round-trip equality on four reference mods stays the correctness bar; extractor/emitter edits bounded to (a) strip-and-regen retirement under `audit-harness` (if first run flags shape deletion), (b) typed `NonSummonEntry` under `nonsummon-typed-ir`, (c) summon prefilter + nested-egg widening under `typed-summon-extractor`, (d) regenerator body-shape corrections under `complete-regenerators` |
| No drift-class parser/emitter fixes outside the four bounded carve-outs | Open PIPELINE_FIDELITY drift classes plus `HiddenModifierType::Skip` placeholder owned by the future `features/pipeline-fidelity/` feature; no chunk in this feature touches them |
| No game-balance, roster, or corpus changes | No chunk modifies `working-mods/*.txt` content; corpus-evidence audits read the four reference mods as they stand |
| No expansion of author-content cross-IR semantic checks beyond deduplicating the two existing collision rules | `xref-context-check-introduction` + `xref-narrowing-and-rename` (the dedupe pair) reassign shared cases to the narrower rule and trim the broader rule; no new author-content rule categories admitted; the audit chunks (`audit-marker` / `audit-marker-retrofit` / `audit-harness`) are a structural check on IR-shape against the corpus, not an author-content rule |

## Architecture summary

The compiler closes nine residual invariants across the typed IR, the cross-IR rule layer, the build-time harvester, the derived-structural regenerator dispatch, and the authority-doc vocabulary. Per `decisions.md` 2026-05-14 decomposition, the work ships as fourteen chunks across three dependency chains plus six independents.

The typed-IR / dedupe chain (`nonsummon-typed-ir` → `nonsummon-typed-authoring` and `nonsummon-typed-ir` → `typed-summon-extractor` → `xref-context-check-introduction` → `xref-narrowing-and-rename`) is the technical core. `nonsummon-typed-ir` lands the SPEC §3.7 atomic retype of `ItempoolItem::NonSummon { name, tier, content }` to `NonSummon(NonSummonEntry)`; `nonsummon-typed-authoring` adds the strict-validation authoring builders (T34 + T30.16c); `typed-summon-extractor` consumes the typed `NonSummonEntry` to route non-matches and lands `Finding.buckets` + `Finding.includes_boss`; the `xref-context-check-introduction` + `xref-narrowing-and-rename` pair dispatches the dedupe predicate on the typed `Finding` fields.

The audit chain (`audit-marker` → `audit-marker-retrofit` → `audit-harness`) lands the variant-evidence invariant. `audit-marker` declares the `InternalStateEnum` opt-out marker trait at `compiler/src/audit_markers.rs` per `decisions.md` 2026-05-14 + 2026-05-13. `audit-marker-retrofit` applies the marker to the three existing Layer-2-reachable internal-state pub enums (`Source`, `StructuralType`, `Severity`) per the partition documented in `decisions.md` 2026-05-13 widening entry. `audit-harness` ships the Layer-1 `syn` walk + Layer-2 `schemars::schema_for!(ModIR)` walk + closed escape-hatch registry + the audit CI test, with the contingent variant-deletion + retirement-protocol bundle landing in the same chunk if first run flags unevidenced shapes.

The harvester chain (`harvester-pure-core` → `harvester-determinism-audit`) lands the byte-determinism invariant. `harvester-pure-core` extracts the pure core into `compiler/src/harvester/` (library-reachable without `std::fs` per SPEC §3.4, with `compiler/build.rs` retained as the I/O shell). `harvester-determinism-audit` lands the in-process double-invocation CI test + lexical denylist + pre-vs-post SHA-256 ledger.

Six chunks ship independent of these chains: `complete-regenerators` lands `generate_pool_replacement` and rewrites `generate_char_selection`'s body inside the same dispatch function in `compiler/src/ir/merge.rs::regenerate_derived_kinds` (and introduces `DerivedKind`); `authorable-new-constructors` adds `::new(identity)` to four authorable IR types without `Default` derive; `authority-doc-vocab-alignment` strips retired IR-identifier names from SPEC §10 glossary heading while preserving the player-facing role description (per decisions.md 2026-05-12) and installs a permanent `compiler/tests/` guard against the retired tokens in authority docs; `summon-name-rename` performs the atomic `target_name` → `summon_name` rename across every callsite.

Cross-chunk contracts the engineering plan introduces — `compiler/src/harvester/` module, `harvest_face_ids` + `harvest_sprites` (existing build-script entry points at `compiler/build.rs` retained at their current names; `harvester-pure-core` extracts the pure core in place), `closed_escape_hatch_registry` (two entries: `FaceIdValue::Unknown` enum variant + unknown-sprite escape hatch; landed by `audit-harness`), `InternalStateEnum` opt-out vehicle marker trait at `compiler/src/audit_markers.rs` (landed by `audit-marker`; applied to internal-state enums by `audit-marker-retrofit` and by every chunk introducing a new ModIR-reachable internal-state pub enum), the two introduced `check_replica_item_in_context` and `check_monster_in_context` per-entity context-checks parallel to `check_hero_in_context` and `check_boss_in_context` (uniform four-CRUD pattern; landed by `xref-context-check-introduction`; `find_name_category` retires fully there), `textmod_compiler::authoring::sprite::SPRITE_REGISTRY` access-path stability, `NonSummonEntry` and `SummonClassification` (declared in `compiler/src/ir/` and `compiler/src/extractor/` respectively; landed by `nonsummon-typed-ir`), `Finding.buckets`, `Finding.includes_boss` (landed by `typed-summon-extractor`), `DerivedKind` (4-variant sub-enum mirroring `is_derived()`'s allowed-set, declared in `compiler/src/ir/mod.rs` by `complete-regenerators`), `summon_name` — are the durable surface every chunk in this feature consumes against.

## Decisions closure

| Decision | Resolution | Citation |
|---|---|---|
| Four-chunk decomposition (audit, harvester, nonsummon, dedupe) | bound — `ir-shape-evidence-audit` → `audit-marker` → `audit-marker-retrofit` → `audit-harness`; `harvester-byte-determinism` → `harvester-pure-core` → `harvester-determinism-audit`; `nonsummon-typed-schema` → `nonsummon-typed-ir` → `nonsummon-typed-authoring` (T34 transitional gap accepted, bounded by immediate-downstream-sibling-same-release-cycle constraint); `single-finding-name-collision` → `xref-context-check-introduction` → `xref-narrowing-and-rename` (brief Goal 1 transitional violation accepted, bounded by immediate-downstream-sibling-same-release-cycle constraint). Supersedes the prior aggregate-surface interpretation of `decisions.md` 2026-05-13 "Audit chunk implementation form" entry per the surface-estimator retraction | decisions.md 2026-05-14 "Decompose four feature-shaped chunks per surface-estimator findings" |
| Chunk landing order across the typed-IR/dedupe chain | bound — `nonsummon-typed-ir` (atomic typed-IR retype) → `typed-summon-extractor` (real classifier + Finding migration + nested-egg widening) → `xref-context-check-introduction` (per-entity context-checks + find_name_category retirement) → `xref-narrowing-and-rename` (V020 narrowing + per-target X003 + label flip + humanize_bucket — delivers brief Goal 1). `nonsummon-typed-authoring` ships as a parallel-to-typed-summon-extractor sibling whose only dependency is `nonsummon-typed-ir`. Other chunks DAG-independent and may land in any order against this chain or each other | decisions.md 2026-05-12 "Chunk landing order" + decisions.md 2026-05-14 "Decompose four feature-shaped chunks" |
| `ReplicaItem` nested-egg schema shape | bound — recursive sum where the enemy slot carries an inner enemy via optional boxed self-reference; strengthens the typed-summon-extractor Goal's property to "every depth-N nesting is structurally encoded; flat sequences excluded" | decisions.md 2026-05-12 "`ReplicaItem` nested-egg schema is a recursive sum" |
| Variant-evidence audit walk depth | bound — recursive transitive closure of input-bytes-determined enums reachable from `ModIR` via field traversal; no per-enum opt-in; new IR-reachable enums covered automatically | decisions.md 2026-05-12 "Variant-evidence audit walks transitive closure of input-bytes-determined enums" |
| `complete-regenerators` chunk scope | bound — atomic chunk landing `generate_pool_replacement` (retires the `_ => {}` deferred wildcard arm in `regenerate_derived_kinds`) AND rewriting `generate_char_selection`'s body to match the corpus shape (per-hero bracketed labels + party-add effect + variable assignment); both regenerator edits share the same `regenerate_derived_kinds` dispatch function and the same four-mod regenerator dogfood | decisions.md 2026-05-12 "Complete-regenerators chunk lands `PoolReplacement` regenerator and `generate_char_selection` body-shape correction atomically" |
| SPEC §10 glossary entry disposition | bound — heading strips the retired IR-identifier names ("Capturable", "Legendary"); player-facing role description survives; chunk-authoring picks the exact replacement heading text; property bound is "no retired IR-identifier names in §10 AND player-facing role description survives" | decisions.md 2026-05-12 "SPEC §10 glossary `Capturable / Legendary` entry retained with retired IR-identifier names stripped from the heading" |
| Audit covers two shape classes; registry mechanism is per-entry | bound — enum-variant walk for the unknown-face-ID entry; name-membership check against build-time `SPRITE_REGISTRY` for the unknown-sprite entry; registry membership remains brief-amendment-only | decisions.md 2026-05-08 "Audit covers two shape classes; registry mechanism is per-entry" |
| Escape-hatch registry closure | bound — exactly two entries (`FaceIdValue::Unknown` enum variant; unknown-sprite escape hatch); future candidates require brief amendment; no per-variant marker scheme honored | decisions.md 2026-05-08 "Escape-hatch registry is closed at two entries" |
| Variant-deletion bundle: class-defined doc-surface grep | bound — chunk-authoring grep regex (case-insensitive, separator-class alternations) against `personas/*.md` + `SPEC.md`; engineering-plan author re-runs at chunk-authoring time and widens further if a current authority-doc prose form misses; regex is the implementation, the class ("every authority document naming the deleted kind") is the binding; inherited retirement-protocol artifacts (retirement comment at site, permanent guard test, doc-surface amendments) ship in the same chunk | decisions.md 2026-05-08 "Variant-deletion bundle regex is case-insensitive and prose-form-tolerant" + 2026-05-08 "Variant-deletion bundle is class-defined" |
| Harvester core invocability and input shape | bound — pure core reachable from `compiler/tests/` without `std::fs` (SPEC §3.4); input shape is `&[(name, contents)]` slices in memory; I/O shell (`fs::read_to_string`, `fs::write`) stays in `compiler/build.rs`; integration tests use `include_str!`/`include_bytes!` for compile-time corpus access | decisions.md 2026-05-08 "Determinism audit requires harvester core invokable outside `cargo build`" |
| Harvester core module placement and reach mechanism | bound — `compiler/src/harvester/` houses the pure core; `compiler/build.rs` reaches it via `#[path = "src/harvester/mod.rs"]` declaration so the build script does not impose a `use crate::` dependency on the library being built; `harvest_face_ids` and `harvest_sprites` are the pure-core entry points (current names in `compiler/build.rs` retained — the chunk extracts the pure core in place without renaming); library-side reach is via `pub mod harvester;` in `compiler/src/lib.rs` so consumers import as `textmod_compiler::harvester::{harvest_face_ids, harvest_sprites}` (stable access-path symmetric to `textmod_compiler::authoring::sprite::SPRITE_REGISTRY`) | author state invocation 1 prosecution finding f5 (architecture) + decisions.md 2026-05-08 |
| Determinism audit detection criterion | bound — detects any harvester input that varies across freshly-spawned OS processes (wall-clock at any resolution, environment, hash-map iteration order, globally-cached random state, file-system iteration order); engineering-plan picks the mechanism (in-process double-invocation + lexical denylist is the chosen vehicle per author state invocation 1 + decisions.md 2026-05-11 amendment) | decisions.md 2026-05-08 "Determinism audit commits to process-distinguishable-input criterion" + 2026-05-11 brief UFC #3 vocabulary alignment |
| `phf_codegen` confinement | bound — `phf_codegen` is a build-dep only; the harvester pure core returns `Vec<(String, String)>` pairs and never imports `phf_codegen`; `compiler/build.rs` glue assembles `phf::Map` via `phf_codegen::Map::build()` from the pure-core's output; the lexical denylist (below) bans `phf_codegen::` inside `compiler/src/harvester/**/*.rs` | author state invocation 1 prosecution finding f4 (architecture) + f1 (backend) |
| Lexical denylist for harvester pure-core determinism audit | bound — denies `std::time::`, `std::env::`, `std::fs::`, `OnceLock`, `OnceCell`, `lazy_static!`, `once_cell::`, `fs::read_dir`, `use crate::`, `use super::`, `crate::` as path-prefix anywhere in file, `use textmod_compiler::`, `phf_codegen::` inside `compiler/src/harvester/**/*.rs`; positive boundary specification — harvester pure-core types limited to primitives + module-local types defined inside `compiler/src/harvester/**/*.rs`; `compiler/build.rs` is in scope of the audit with an explicit allowlist for the bound I/O-shell surface | decisions.md 2026-05-11 amendment "Process-distinguishable-input verification mechanism denylist clause" + author state invocation 1 prosecution finding f2 / f3 (testing) |
| Pre-vs-post byte-identity SHA-256 ledger | bound — `harvester-pure-core` chunk's acceptance criterion captures a pre-refactor SHA-256 of `face_id_generated.rs` and `sprite_registry_generated.rs` before any pure-core extraction; if post-refactor SHA differs, the chunk is REJECTED (divergence indicates pre-existing latent non-determinism or out-of-scope structural change); the ledger artifact is the chunk's PR-attached test evidence (per decisions.md 2026-05-14 "Harvester SHA ledger consolidation" — the one-time check lives entirely in `harvester-pure-core`, not in `harvester-determinism-audit`) | author state invocation 1 prosecution finding f3 (backend) / f5 (testing) + decisions.md 2026-05-11 round-1 resolution + decisions.md 2026-05-14 "Harvester SHA ledger consolidation" |
| Vehicle marker semantics (opt-out, not opt-in) | bound — the vehicle marker attests "NOT input-bytes-determined, exempt from corpus-evidence walk"; the audit's default for every reachable pub enum in scope is "must be corpus-evidenced or registry-listed". The marker is a per-enum opt-OUT applied at the declaration site for compiler-internal-state enums that are Layer-2-reachable from `ModIR` (e.g. `pub enum Source` reachable via `Finding.source`, `pub enum StructuralType` reachable via `StructuralModifier.modifier_type`, `pub enum Severity` reachable via `Finding.severity` — enums whose variant choice is determined by compiler internal state rather than input bytes per the corpus-evidence Goal's carve-out "provenance tags, classifier tags, and source filters today"; Layer-2-unreachable internal-state enums such as `pub enum SourceFilter` (held only on `BuildOptions`, not a `ModIR` field) and `pub enum ErrorKind` (held only on `CompilerError`) are carve-outs per `decisions.md` 2026-05-13 Layer-1 widening — they require no marker attachment because the audit's Layer-2 reachability walk does not reach them). The closed escape-hatch registry remains at exactly two entries (`FaceIdValue::Unknown` enum variant + unknown-sprite escape hatch) per decisions.md 2026-05-08; the registry is the brief-amendment-only carve-out for input-bytes-determined shapes that legitimately lack corpus evidence. Inverting semantics this way preserves the audit's role as a brief-anchored invariant: a new input-bytes-determined pub enum landing without explicit classification fails closed (default audit walk), not silently miss-passes | decisions.md 2026-05-14 "Vehicle marker semantics (opt-out, not opt-in)" (durable promotion) + round-4 reviewer state IMPLEMENTABILITY_GAP "Input-bytes-determined vs internal-state enum classification mechanism" resolution + the corpus-evidence Goal's carve-out enumeration |
| Existing pub enum partition retrofit scope | bound — `audit-marker-retrofit` applies the opt-out marker to the existing Layer-2-reachable internal-state pub enums (today `Source`, `StructuralType`, `Severity`); `audit-harness`'s first run enumerates every existing pub enum reachable from `ModIR` inside `compiler/src/**/*.rs` (excluding `compiler/tests/` and `compiler/build.rs`; scope widened per `decisions.md` 2026-05-13 "Audit Layer-1 scope widened from four directories to `compiler/src/**/*.rs`") and classifies each as input-bytes-determined OR internal-state per the corpus-evidence Goal's carve-out vocabulary; input-bytes-determined enums without corpus evidence enter the per-variant decisions-log-entry + retirement-protocol bundle path (decisions.md 2026-05-08) inside `audit-harness`'s own chunk. The audit harness lands green at first run with this partition complete | round-4 reviewer state IMPLEMENTABILITY_GAP partition mechanism resolution + `decisions.md` 2026-05-13 Layer-1 scope widening + decisions.md 2026-05-14 "Decompose four feature-shaped chunks" |
| Deterministic variant-enumeration vehicle (for the audit's enumeration CI test) | bound — `audit-harness`'s CI test enumerates every `pub enum` declaration inside `compiler/src/**/*.rs` (excluding `compiler/tests/` and `compiler/build.rs`; scope matches the Existing pub enum partition retrofit row per decisions.md 2026-05-13 Layer-1 widening) and applies the opt-out-marker check per the inverted semantics row above. The Layer-1 walk mechanism is bound by decisions.md 2026-05-13 "Audit chunk implementation form" as `syn` source-parse over the in-scope universe; the Layer-2 reachability walk is `schemars::schema_for!(ModIR)` per the same entry. The audit harness's enforcement is per-CI-run (every chunk PR that touches an in-scope file re-runs the audit), so a directory that materializes mid-feature (e.g. `compiler/src/harvester/` from `harvester-pure-core`) carries marker enforcement from the moment that chunk's PR opens | author state invocation 1 prosecution finding f4 (backend) + decisions.md 2026-05-11 amendment "In-scope-enum walk mechanism" + decisions.md 2026-05-13 "Audit chunk implementation form" + decisions.md 2026-05-13 "Audit Layer-1 scope widened" |
| Cross-chunk vehicle-attachment ownership | bound — `audit-marker` lands the opt-out vehicle marker trait declaration; `audit-marker-retrofit` applies the marker to the existing Layer-2-reachable internal-state pub enums and documents the partition; `audit-harness` lands the enumeration CI test + audit harness, and on first run the per-variant decisions-log-entry + retirement-protocol bundle on any unevidenced input-bytes-determined shape. Every other chunk introducing a new `pub enum` reachable from `ModIR` MUST classify the new enum at landing time (apply the opt-out marker for an internal-state enum; leave unmarked for an input-bytes-determined enum, which then must be corpus-evidenced before the chunk's audit run lands green). The audit's CI test enforces this on every CI run; a future chunk that lands a new pub enum reachable from `ModIR` whose semantics it omits to classify fails CI in that chunk, not three rounds later | round-4 reviewer state IMPLEMENTABILITY_GAP "Vehicle-attachment obligation cross-chunk dependency" resolution path (A) + decisions.md 2026-05-14 "Decompose four feature-shaped chunks" |
| `nonsummon-typed-ir` + `nonsummon-typed-authoring` chunk plan location and authority | bound — `/plan-author --rewrite` against the existing `features/compiler-invariant-completions/implementation/nonsummon-typed-schema.md` produces TWO co-located chunk plans (`nonsummon-typed-ir.md` + `nonsummon-typed-authoring.md`) that REPLACE the existing file; the per-chunk decisions sidecar `nonsummon-typed-schema-decisions.md` carries forward into the rewrite's chunk-internal arbitration (the rewrite may itself split or relocate the sidecar's content as needed); chunk-plan normalization to this feature's chunk-plan format happens via `/plan-author --rewrite` before nonsummon-typed-ir authors | decisions.md 2026-05-11 "Goal 4 added" + 2026-05-13 "platform-foundations retirement + sidecar relocation" + 2026-05-14 "Decompose four feature-shaped chunks" + 2026-05-14 "Nonsummon chunk-plan file shape" |
| V020 narrowing predicate shape and emission-site universe | bound — narrowing predicate is "the colliding category set is a subset of `{hero, replica_item, monster}` with cardinality ≥2", factored into a shared helper called from every xref.rs emission surface: `check_cross_category_names` (global pass), and every branch of all four per-entity context-checks — `check_hero_in_context`'s three branches (hero-vs-replica_item, hero-vs-monster, hero-vs-boss), the newly-introduced `check_replica_item_in_context`'s three branches (replica_item-vs-hero, replica_item-vs-monster, replica_item-vs-boss), the newly-introduced `check_monster_in_context`'s three branches (monster-vs-hero, monster-vs-replica_item, monster-vs-boss), and `check_boss_in_context`'s three branches (boss-vs-hero, boss-vs-replica_item, boss-vs-monster). The predicate is TRUE at the six non-boss-involving sites (hero-vs-replica_item, hero-vs-monster, replica_item-vs-hero, replica_item-vs-monster, monster-vs-hero, monster-vs-replica_item) where the per-target X003 form fires and V020 is narrowed away; at the six boss-involving sites (all `check_boss_in_context` branches plus the `*-vs-boss` branch of each of the other three context-checks) the predicate is structurally a no-op (colliding set always includes `boss`) and V020 fires. The call to the shared helper is retained at every site so a future fifth-category extension preserves the disjoint-by-construction property without per-surface re-deriving the asymmetry | decisions.md 2026-05-11 "V020 narrowing covers every xref.rs emission site" + round-4 reviewer state per-surface asymmetry OPEN_QUESTION resolution + round-5 reviewer state add_monster gap OPEN_QUESTION resolution path (A) |
| CRUD-mutator surface delivers per-target X003 on `{hero, replica_item, monster}` subset collisions | bound — `xref-context-check-introduction` introduces `check_replica_item_in_context` and `check_monster_in_context` in `compiler/src/xref.rs`, parallel to the existing `check_hero_in_context` and `check_boss_in_context`; `xref-narrowing-and-rename` lands the V020 narrowing predicate + per-target X003 routing + `humanize_bucket` + the `"legendary"` → `"replica_item"` rename on top. Every `ModIR::add_*` CRUD op invokes its matching context-check as the sole pre-add validation gate; the private `find_name_category` helper retires fully (zero callers, retirement-protocol guard test per CLAUDE.md "Retiring a public identifier"). The standalone color-uniqueness check in `add_hero` retires alongside `find_name_category`'s invocation there, since `check_hero_in_context` already emits V019 for color collisions — keeping both alive would fire two findings on the same color-collision defect, contradicting the dedupe Goal's exactly-one-finding property. The intra-replica trigger-discriminant check inside `add_replica_item` (keyed on `(target_name, mem::discriminant(&trigger))`) survives because the context-check operates on `&ReplicaItem` + `&ModIR` and cannot see intra-bucket trigger-discriminant collisions on the to-be-added item. At the CRUD surface, a `{hero, replica_item, monster}` subset collision produces exactly one X003 finding regardless of which add-op the caller invoked; a boss-involving collision produces exactly one V020 finding; intra-bucket duplicates produce the broader rule's existing `CompilerError::duplicate_name` short-circuit (preserved on the intra-replica trigger-discriminant path only). The per-target X003 form and the existing full-IR `check_duplicate_pokemon_buckets` share a single internal owner-map-and-bucket-collision predicate. Chunk-internal: the `ValidationReport`→`CompilerError` conversion-helper signature, the per-CRUD-op retirement procedure, and the test-case scaffolding live in the chunk plan. | round-4 reviewer state UNCORROBORATED_RESET resolution path (A) + round-5 reviewer state FIX_INTRODUCED_PREMISE_INVERSION color-check parallel-paths resolution path (A) + round-5 reviewer state OPEN_QUESTION add_monster gap resolution path (A) + architecture persona ground-truth grep on `compiler/src/ir/ops.rs::add_hero` / `add_replica_item` / `add_monster` / `add_boss` + `compiler/src/xref.rs::check_hero_in_context` V019 emission verified |
| X003 owner-map bucket label and user-facing vocabulary | bound — owner-map literal flips from `"legendary"` to `"replica_item"` (snake_case code identifier matching bucket-field names like `hero`, `monster`, `boss`); user-facing X003 message and suggestion render the bucket vocabulary via a new `humanize_bucket(&'static str) -> &'static str` helper that maps `"replica_item"` → `"replica item"` (lowercase, space, plural-aligned to SPEC §6.3) at every `buckets.join(...)` call site inside `check_duplicate_pokemon_buckets`. The split keeps code identifiers snake_case while keeping user-facing prose anchored to SPEC §6.3's vocabulary; the bound `rg "legendary" compiler/src/xref.rs` returns-zero exit criterion remains. The retirement-protocol guard test inherits from CLAUDE.md "Retiring a public identifier" — a permanent `compiler/tests/` test greps `legendary` across `compiler/src/**/*.rs` asserting zero hits modulo a documented `compiler/src/builder/mod.rs` historical-anchor exception (chunk-authoring picks whether the historical-anchor comment flips or carries a retirement comment) | decisions.md 2026-05-11 "X003 owner-map bucket label `legendary` → `replica_item`" + round-4 reviewer state OPEN_QUESTION vocabulary humanizer resolution path (a) |
| `SPRITE_REGISTRY` access-path stability | bound — `textmod_compiler::authoring::sprite::SPRITE_REGISTRY` is the stable import path; `audit-harness`'s unknown-sprite escape-hatch detection consumes the registry via this path; the harvester pure-core in `compiler/src/harvester/` returns the `(name, img_data)` pairs that `compiler/build.rs` feeds to `phf_codegen` to populate the registry; the access path is forward-binding for any future audit / xref consumer | author state invocation 1 prosecution finding f2 (architecture) |
| Variant-evidence audit re-run after every chunk that adds an input-bytes-determined IR shape | bound — after a chunk lands new pub IR enums reachable from `ModIR`, the audit's transitive walk auto-covers them; the chunk's CI invocation of `cargo test` exercises the audit harness on the new shapes; manual gate "variant-evidence re-run after rebaseline" in Manual gates section binds the operator runbook step | author state invocation 1 prosecution finding f6 (architecture) |
| `NonSummonEntry::Unclassified` Finding shape (SPEC §3.3 permissive-extract paired Finding) | bound — when the typed classifier in `typed-summon-extractor` routes a novel non-summon shape to `NonSummonEntry::Unclassified` (and during the transitional window the `nonsummon-typed-ir` stub also produces `Unclassified` per decisions.md 2026-05-13), the paired `Finding` carries `field_path` populated with the entry's position in the itempool body (e.g. `structural[N].itempool.entry[M]`) and `suggestion` populated with the next-shape-discovery step (e.g. `"add a typed variant to NonSummonEntry covering this shape"`) per SPEC §5 error-surface contract; the chunk plan's T30.0 Unclassified-budget ratchet test asserts this paired-Finding shape | round-4 reviewer state OPEN_QUESTION "Goal 4 user-facing-outcome verifier gap" resolution |
| Finding typed cross-bucket field shape | bound — `Finding` gains `pub buckets: Vec<&'static str>` and `pub includes_boss: bool` fields (the buckets vector is the source-of-truth for collision-detection rule dispatch; `includes_boss` is the convenience predicate); `typed-summon-extractor` lands the shape change, migrates every `Finding` construction site, and removes every `message.contains(<bucket-label>)` predicate from the test suite (per the typed-summon-extractor Goal verifier) | the typed-summon-extractor Goal verifier "Cross-IR `Finding` widens with typed cross-bucket fields ... rules dispatch on those typed fields" + brief UFC: "Cross-IR `Finding` values carry typed `buckets: Vec<&'static str>` and `includes_boss: bool` fields" |
| Round-trip baseline rebaseline trigger | bound — chunks that change extractor/emitter output shapes (`nonsummon-typed-ir`, `typed-summon-extractor`, `complete-regenerators`) re-record `compiler/tests/baselines/roundtrip/<mod>.baseline` as part of the chunk's PR; `summon-name-rename` is excluded because the roundtrip baseline format is a text-summary (per-line `heroes.count:`, `roundtrip.equal:`, etc., not JSON-serialized field names) that an in-Rust field rename does not change; `audit-harness` re-runs against the rebaselined corpus to confirm no newly-evidenced variants surface as unexpected; manual gate "variant-evidence audit re-run after rebaseline" binds the operator runbook step | author state invocation 1 prosecution finding f6 (architecture) + Manual gates section below + arch persona f5 ground-truth grep on `compiler/tests/baselines/roundtrip/sliceymon.baseline` |

## Invariants

### Single finding per cross-IR defect at every emission surface

The two cross-IR collision-detection rules (V020 cross-category-names, X003 owner-map-bucket-collision) have disjoint-by-construction domains across every xref.rs emission surface (`check_cross_category_names` and every branch of all four per-entity context-checks: `check_hero_in_context`, the newly-introduced `check_replica_item_in_context`, the newly-introduced `check_monster_in_context`, and `check_boss_in_context`) AND at the lib's single-item CRUD surface (every one of `ModIR::add_hero` / `add_replica_item` / `add_monster` / `add_boss` invokes its matching context-check as the sole pre-add validation gate; the private `find_name_category` helper retires fully). A name collision across `{hero, replica_item, monster}` produces exactly one X003 finding regardless of how many categories share it; a boss-involving collision or intra-bucket duplicate produces exactly one V020 finding; a hero color collision produces exactly one V019 finding (emitted only from `check_hero_in_context`, the standalone color-uniqueness check in `ModIR::add_hero` having retired). The narrowing predicate ("colliding category set is subset of `{hero, replica_item, monster}` with cardinality ≥2") is factored into a shared helper invoked from every site. Enforced by `xref-context-check-introduction` (per-entity context-check introduction + CRUD routing + `find_name_category` retirement) and `xref-narrowing-and-rename` (V020 narrowing predicate + per-target X003 routing + `humanize_bucket` + `"legendary"` retirement); the dedupe Goal's exactly-one-finding property lands when `xref-narrowing-and-rename` merges.

### Corpus-evidenced IR shapes via transitive walk

Every pub `enum` whose variant choice depends on input mod bytes, reachable transitively from `ModIR` via field traversal, is either corpus-evidenced (the audit walks `working-mods/{sliceymon,pansaer,punpuns,community}.txt` and finds an instance) or present in the closed escape-hatch registry (`FaceIdValue::Unknown` + unknown-sprite). New IR-reachable input-bytes-determined enums are audit-covered automatically; per-enum opt-in is forbidden. The audit fails closed when an in-scope shipping shape passes neither test. **Audit Layer-2 prerequisite:** every type reachable from `ModIR` via field traversal derives `schemars::JsonSchema` — the audit's reachability walk uses `schemars::schema_for!(ModIR)`'s `definitions` map to enumerate reachable enums, so a reachable type without the derive falls out of the walk and silently misses coverage; `audit-harness`'s chunk plan installs a permanent guard against `ModIR`-reachable types lacking the derive (mechanism — guard test, audit refusal, or other — chunk-internal). Enforced by the audit chain (`audit-marker` lands the marker trait; `audit-marker-retrofit` applies it to existing Layer-2-reachable internal-state enums; `audit-harness` lands the CI test + first-run retrofit + escape-hatch-registry consultation).

### Byte-deterministic generated source

`face_id_generated.rs` and `sprite_registry_generated.rs` (the two harvester-emitted files) are byte-identical across repeated harvester invocations against the same corpus. The harvester pure core in `compiler/src/harvester/` is library-importable without `std::fs`; the lexical denylist + `compiler/build.rs` allowlist forbid wall-clock, environment, hash-map iteration, OnceLock, and other process-distinguishable inputs. Enforced by `harvester-pure-core` (pure-core extraction + one-time pre-vs-post SHA-256 divergence check per decisions.md 2026-05-14) and `harvester-determinism-audit` (recurring CI in-process double-invocation test + lexical denylist on `compiler/src/harvester/**/*.rs`).

### No raw-passthrough IR payloads

No IR variant carries a `String` / `Vec<u8>` / `serde_json::Value` blob holding bytes the parser refused to type. The current `ItempoolItem::NonSummon { name: String, tier: Option<i8>, content: String }` variant declaration in `compiler/src/ir/mod.rs` retypes to `NonSummon(NonSummonEntry)` — a fully typed recursive sum with a permissive-extract `Unclassified` variant whose paired `Finding` populates `field_path` + `suggestion` per SPEC §5 (not a raw escape). The retype retires today's stub-sentinel protocol atomically — the empty-`name` + `None`-`tier` + non-empty-`content` combination produced by the stub body inside `extract_from_itempool` in `compiler/src/extractor/replica_item_parser.rs` doubles as a sentinel that the `emit_itempool` function's `NonSummon`-arm sentinel-detection branch in `compiler/src/builder/replica_item_emitter.rs` detects to skip the `.n.<name>.tier.<n>` prefix synthesis it would otherwise emit; that sentinel-detection branch retires in the same chunk. Enforced by `nonsummon-typed-ir` (atomic typed-IR retype + emitter migration + stub-sentinel retirement); the SPEC §3.3 strict-authoring complement (T34 + T30.16c) lands in the immediate downstream sibling `nonsummon-typed-authoring`. Extension to the typed payload variants is automatic under the corpus-evidenced IR shapes invariant above.

### Derived-structural dispatch completeness

`complete-regenerators` introduces a new `pub enum DerivedKind { Selector, HeroPoolBase, PoolReplacement, ItemPool }` declared in `compiler/src/ir/mod.rs` alongside `StructuralType` — a 4-variant sub-enum that becomes the **single source-of-truth** for which `StructuralType` variants are derived. `DerivedKind` carries a `TryFrom<StructuralType>` impl that succeeds for exactly those four variants and fails (`Err`) for the other 14 of `StructuralType`'s 18 variants, plus an inverse `From<DerivedKind> for StructuralType` for symmetry. `StructuralModifier::is_derived()` is rewritten to delegate to this `TryFrom` — its body becomes `self.derived && DerivedKind::try_from(&self.modifier_type).is_ok()`, retiring the existing 4-variant `matches!` literal so the predicate consults `DerivedKind`'s variant set rather than its own parallel encoding (per decisions.md 2026-05-13 "`DerivedKind` is single source-of-truth; `is_derived()` delegates"). The dispatch in `compiler/src/ir/merge.rs::regenerate_derived_kinds` splits into a thin outer function (filters `StructuralType` → `DerivedKind` via the `TryFrom` impl; the outer arm on `Err` is `unreachable!()` since the `kinds` argument is built upstream by `compiler/src/ir/merge.rs::collect_stripped_kinds`, which filters by `StructuralModifier::is_derived()` — and `is_derived()` returns true only when `DerivedKind::try_from` succeeds — so any reached `Err` indicates the `is_derived()`-delegates-to-`DerivedKind::try_from` discipline has been broken and surfaces as a runtime panic at first test invocation rather than as a silent no-op) and an inner function exhaustive over `DerivedKind`'s four variants — Rust's compiler-enforced exhaustiveness check on `DerivedKind` is the property's load-bearing enforcer. The wildcard `_ => {}` arm inside `regenerate_derived_kinds` retires; flipping a fifth `StructuralType` variant to derived requires only one step — extending `DerivedKind` with a corresponding variant — which then forces the inner exhaustive-match arm at compile time AND auto-flips `is_derived()`'s output for the new variant (because `is_derived()` consults `DerivedKind::try_from`). Enforced by `complete-regenerators` (DerivedKind + TryFrom + inverse From + outer/inner split + `is_derived()` delegation rewrite + a guard test in `compiler/tests/` that constructs a `StructuralModifier` for every variant `is_derived()` returns true on and asserts each regenerates without panic or sink, complementing the compile-time variant-coverage check with per-arm correctness verification).

### Cross-chunk vehicle classification on every pub enum reachable from ModIR

Every `pub enum` declaration inside `compiler/src/**/*.rs` (excluding `compiler/tests/` and `compiler/build.rs`; scope widened per `decisions.md` 2026-05-13 "Audit Layer-1 scope widened from four directories to `compiler/src/**/*.rs`") reachable from `ModIR` is classified at chunk-landing time as either input-bytes-determined (no marker; variants must be corpus-evidenced or registered) or compiler-internal-state (opt-out vehicle marker applied at declaration site per the bound semantics row in Decisions closure). The audit harness's enumeration CI test fails when a reachable pub enum is neither marked nor corpus-evidenced. **Reachability carve-out:** pub enums declared in the in-scope directories but never stored on a `ModIR` field (used only as transient discriminators inside helper functions — `DerivedKind` per decisions.md 2026-05-13 "`DerivedKind` is single source-of-truth" is the round-7 example) are unreachable from `ModIR` via field traversal, fall out of Layer-2's schemars walk, and are audit-exempt; their audit-exempt disposition is recorded explicitly in `REACHABILITY_CARVE_OUT` inside `audit-harness`'s test file (the registry where Layer-2-unreachable in-scope pub enums are intentionally enumerated; the chunk introducing such an enum extends the list in its own PR per decisions.md 2026-05-13 "`DerivedKind` classification disposition independent of merge order"). The chunk introducing a Layer-2-unreachable enum carries no marker-attachment obligation, but it does carry the `REACHABILITY_CARVE_OUT` extension obligation (or `audit-harness`'s own first run seeds the list, depending on merge order). Enforced by the audit chain (`audit-marker` lands the marker trait; `audit-marker-retrofit` applies it to existing Layer-2-reachable internal-state enums; `audit-harness` lands the CI test + the `REACHABILITY_CARVE_OUT` registry + the same-chunk first-run retrofit). Every other chunk that introduces a new `pub enum` reachable from `ModIR` classifies the new enum on landing; every chunk introducing a Layer-2-unreachable enum extends `REACHABILITY_CARVE_OUT` on landing.

### Authority-doc vocabulary matches shipped IR

`SPEC.md` carries no retired IR-identifier names as live shape identifiers. The §10 glossary entry's heading strips "Capturable" and "Legendary" (the player-facing role description survives); §6.3's "at most one of: heroes, replica items, monsters" uniqueness prose survives verbatim. A permanent `compiler/tests/` guard test greps the retired parentheticals + classifier-ASCII tokens against `SPEC.md` and asserts zero hits. Enforced by `authority-doc-vocab-alignment`.

## User-facing vocabulary humanizer

`compiler/src/xref.rs` introduces `pub fn humanize_bucket(bucket: &'static str) -> &'static str` mapping snake_case bucket-identifier codes (`hero`, `replica_item`, `monster`, `boss`) to SPEC-anchored user-facing prose (`"hero"`, `"replica item"`, `"monster"`, `"boss"`). Every `buckets.join(...)` site in the cross-IR rule layer maps through this helper before rendering. The helper is the single source-of-truth for code-identifier-to-user-facing-vocabulary mapping; future bucket-label renames or vocabulary-prose changes land at this site only.

## Build determinism

`compiler/build.rs` reaches the harvester pure core at `compiler/src/harvester/` via `#[path = "src/harvester/mod.rs"]` declaration. The pure core returns `Vec<(String, String)>` pairs for the face-ID and sprite registries; `compiler/build.rs` glue assembles `phf::Map` via `phf_codegen::Map::build()` and writes `face_id_generated.rs` + `sprite_registry_generated.rs`. The pure core takes `&[(name, contents)]` slices in memory (no `Path` arguments, no `std::fs`). Two harvester invocations against the same corpus produce byte-identical output; `harvester-determinism-audit`'s `audit_harvester_determinism.rs` integration test exercises this property in-process. The build-time SHA-256 ledger captured during `harvester-pure-core`'s pre-vs-post refactor pass is `harvester-pure-core`'s PR-attached test evidence (per decisions.md 2026-05-14 "Harvester SHA ledger consolidation").

## Source precedence and finding lanes

`Source::{Base, Custom, Overlay}` ships on main and drives build emission filter (`BuildOptions::include`), finding severity promotion (`promote_severity`), and merge strip path. No chunk in this feature modifies the `Source` discipline; chunks that emit new `Finding` values populate `source: Option<Source>` per the existing platform contract in `compiler/src/ir/mod.rs`.

## Manual gates

| Gate | Blocks on |
|---|---|
| Four-mod round-trip baseline re-record | `nonsummon-typed-ir`, `typed-summon-extractor`, `complete-regenerators` (chunks that change extractor/emitter output shapes); `cargo run --example roundtrip_diag` then `compiler/tests/baselines/roundtrip/<mod>.baseline` re-record |
| Byte-determinism re-run after harvester edits | `harvester-pure-core` carries the one-time pre-vs-post SHA-256 divergence check within its own PR (rejection on divergence per decisions.md 2026-05-11 round-1 resolution + 2026-05-14 "Harvester SHA ledger consolidation"); `harvester-determinism-audit`'s in-process integration test is the recurring CI gate that runs on every subsequent CI invocation and gates any future change to `compiler/build.rs` or `compiler/src/harvester/**/*.rs` (per decisions.md 2026-05-08 the audit invokes the harvester pure core in-process via `include_str!`-baked corpus) |
| Variant-evidence audit re-run after rebaseline | After any chunk that lands new pub IR enums reachable from `ModIR` (`nonsummon-typed-ir`, `typed-summon-extractor`); the audit harness in `audit-harness` re-runs via `cargo test` to confirm the transitive walk picks up new enums and the closed escape-hatch registry covers everything not corpus-evidenced |
| Authority-doc grep audit | After `authority-doc-vocab-alignment` lands and after `xref-narrowing-and-rename`'s xref.rs `legendary` retirement; chunk-authoring grep (case-insensitive, separator-class regex per decisions.md 2026-05-08) re-runs against `personas/*.md` + `SPEC.md` to confirm no retired IR-identifier prose surfaces; permanent guard tests in `compiler/tests/` enforce this on every CI run |

Each `--apply` requires:
- Pre-PR `cargo test` passes (including the new audit harnesses on landed chunks)
- Pre-PR `cargo run --example roundtrip_diag` shows `extract(build(extract(mod))) == extract(mod)` on all four reference mods
- Pre-PR SHA-256 ledger for `harvester-pure-core` attached to the chunk's PR

## Chunk index

| Slug | Description | Code deps |
|---|---|---|
| `audit-marker` | Declares `pub trait InternalStateEnum {}` at `compiler/src/audit_markers.rs` per decisions.md 2026-05-14 + 2026-05-13 "Audit chunk implementation form" (marker form binding). Includes rustdoc semantics doc-comment per decisions.md 2026-05-14 marker-misuse mitigation (the mandatory rustdoc-on-impl discipline mechanically checked downstream by `audit-harness`'s `syn`-walker `ItemImpl.attrs` inspection). Cross-chunk contract: `InternalStateEnum` symbol available for every chunk landing internal-state pub enums reachable from `ModIR`. No retrofit, no audit harness in this chunk. | — |
| `audit-marker-retrofit` | Applies `impl InternalStateEnum for X {}` to the three existing Layer-2-reachable internal-state pub enums: `Source` (reachable via `Finding.source`), `StructuralType` (reachable via `StructuralModifier.modifier_type`), `Severity` (reachable via `Finding.severity`). Each impl carries the rustdoc-on-impl doc-comment per decisions.md 2026-05-14 marker-misuse mitigation. Documents the partition (which existing pub enums are Layer-2-reachable and need the marker vs Layer-2-unreachable carve-outs like `SourceFilter` and `ErrorKind` that need no marker per decisions.md 2026-05-13 widening). Chunk-internal: exact rustdoc text per impl + partition tables live in the chunk plan. | `audit-marker` |
| `audit-harness` | Audit CI test in `compiler/tests/`. Layer-1 `syn` source-parse walk over `compiler/src/**/*.rs` (excluding `compiler/tests/` and `compiler/build.rs` per decisions.md 2026-05-13 Layer-1 widening) discovers every `pub enum X` declaration AND every `impl InternalStateEnum for X` impl; Layer-2 `schemars::schema_for!(ModIR)` walk identifies the subset reachable from `ModIR` via the schema definitions map. Closed escape-hatch registry holds two entries (`FaceIdValue::Unknown` enum variant + unknown-sprite escape hatch per decisions.md 2026-05-08); the audit fails closed on reachable-and-unmarked-and-unevidenced enums. If first run identifies unevidenced input-bytes-determined shapes the same chunk lands per-variant decisions.md entries + the variant deletion + every rule/test referencing the variant + the inherited retirement-protocol bundle (retirement comment at site + permanent guard test + class-defined doc-surface amendments per the chunk-authoring grep against `personas/*.md` + `SPEC.md` per decisions.md 2026-05-08). Cross-chunk contract: per-variant evidence-corpus extension obligation — every downstream chunk landing new input-bytes-determined IR enums extends the audit's evidence corpus in the same chunk; the representation is chunk-internal. | `audit-marker-retrofit` |
| `harvester-pure-core` | Extracts the harvester pure core into `compiler/src/harvester/` reachable from library code without `std::fs` (SPEC §3.4). `compiler/build.rs` retains `#[path = "src/harvester/mod.rs"]` reach + I/O shell (`fs::read_to_string` for corpus bytes, `fs::write` for generated files, `phf_codegen::Map::build()` for registry assembly). Pure-core entry points `harvest_face_ids` and `harvest_sprites` keep their current names (the existing `compiler/build.rs` callers continue to invoke them at the same names). Pure-core takes `&[(name, contents)]` slices in memory (no `Path` arguments, no `std::fs`) and returns `Vec<(String, String)>` pairs. `phf_codegen` confinement boundary established: pure-core never imports `phf_codegen` per decisions.md 2026-05-11. **One-time pre-vs-post SHA-256 divergence check:** the chunk captures the pre-refactor SHA-256 of `face_id_generated.rs` + `sprite_registry_generated.rs` at its opening, runs the extraction, captures the post-refactor SHA at its closing, compares them in its own acceptance criterion, and is REJECTED on divergence per decisions.md 2026-05-11 round-1 resolution (divergence indicates pre-existing latent non-determinism or out-of-scope structural change). The ledger artifact is the chunk's PR-attached test evidence. Cross-chunk contract: harvester pure-core function signatures reachable from library code (consumed by `harvester-determinism-audit` for the recurring CI test + by the future browser/mobile mod-builder app feature). | — |
| `harvester-determinism-audit` | `audit_harvester_determinism.rs` integration test in `compiler/tests/` exercises in-process double-invocation of the harvester pure core against `include_str!`-baked corpus (compile-time corpus bytes, no runtime `std::fs`). This is the **recurring CI determinism gate** that runs on every subsequent CI run, asserting any future regression introducing process-distinguishable input (wall-clock, environment, hash-map iteration order, etc.) fails the test. The one-time refactor-time SHA-256 divergence check is NOT carried here — it lives entirely inside `harvester-pure-core`'s PR per decisions.md 2026-05-14 ("Harvester SHA ledger consolidation"). Lexical denylist on `compiler/src/harvester/**/*.rs` per decisions.md 2026-05-11 amendment denies `std::time::`, `std::env::`, `std::fs::`, `OnceLock`, `OnceCell`, `lazy_static!`, `once_cell::`, `fs::read_dir`, `use crate::`, `use super::`, `crate::` as path-prefix, `use textmod_compiler::`, `phf_codegen::`. Explicit allowlist on `compiler/build.rs` I/O shell. | `harvester-pure-core` |
| `complete-regenerators` | `generate_pool_replacement` regenerator + corrected `generate_char_selection` body shape (per-hero bracketed labels + party-add effect + variable assignment matching the corpus form). Introduces `pub enum DerivedKind { Selector, HeroPoolBase, PoolReplacement, ItemPool }` in `compiler/src/ir/mod.rs` as the **single source-of-truth** for which `StructuralType` variants are derived; `StructuralModifier::is_derived()` is rewritten to delegate to `DerivedKind::try_from` per decisions.md 2026-05-13. The `_ => {}` wildcard arm in `regenerate_derived_kinds` retires; the outer/inner dispatch split makes the `Err` arm structurally unreachable (see Invariants § Derived-structural dispatch completeness). The complete-regenerators Goal verifier: per-regenerator source-vs-IR divergence tests prove each regenerator reads from content (the hero list, the replica-item list, the structural-modifier set) rather than from a registry or canonical source; both regenerators verified against the four reference mods byte-for-byte. Chunk-internal: retirement-protocol guard test for the retired wildcard arm, per-arm correctness guard test, and the exact shape of the outer/inner dispatch refactor live in the chunk plan. | — |
| `authorable-new-constructors` | `::new(identity)` constructors on `HeroBlock`, `ReplicaItem`, `AbilityData`, `TriggerHpDef` (all declared in `compiler/src/ir/mod.rs`) with required identity-bearing arguments. The four types already lack `Default` derive today; the chunk lands a permanent `compiler/tests/` guard test that asserts `Default` is not implemented for any of the four (locking the is-and-stays property forward). The chunk's work is the constructors + the guard test, not a retirement of existing `Default` impls. The existing `AbilityData::parse` and `TriggerHpDef::parse` methods are preserved unchanged — the brief's "an emitter cannot receive an instance whose required identity bytes are empty" property is enforced on the new `::new` path; `parse()` remains the extractor-only ingestion shape. Chunk-internal: exact constructor signatures + guard-test mechanism (e.g. `static_assertions::assert_not_impl_all!` or analogous) live in the chunk plan. | — |
| `summon-name-rename` | Atomic rename of `ReplicaItem.target_name` to `ReplicaItem.summon_name` across every callsite (`compiler/src/**/*.rs`, tests, JSON-snapshot baselines); permanent retirement-protocol guard test greps `target_name` against `compiler/src/**/*.rs` asserting zero hits modulo documented exceptions | — |
| `authority-doc-vocab-alignment` | Strip retired IR-identifier names ("Capturable", "Legendary") from authority-doc prose surfaces — SPEC §10 glossary entry heading per decisions.md 2026-05-12 (player-facing role description survives), SPEC overview prose naming "heroes / capturables / monsters / bosses", and `personas/slice-and-dice-design.md`'s corpus-uniqueness-check pattern naming "hero, monster, OR capturable". Chunk-authoring grep (case-insensitive separator-class regex covering "Capturable" plural form + "Legendary"/"Legendaries" + lowercase forms, distinct from the derived-structural regex in decisions.md 2026-05-08) re-runs against `SPEC.md` + `personas/*.md` and amends every match naming retired kinds as live shape. Documented preserved-vocabulary exceptions: (i) SPEC §10 player-facing role-description body; (ii) `personas/slice-and-dice-design.md`'s `Legendary` design-vocabulary heading (player-facing role bound to survive); (iii) the `legendary` literal in `personas/slice-and-dice-design.md`'s Tyranitar pseudo-legendary Pokemon-trivia entry per decisions.md 2026-05-11 X003 owner-map row exclusion. Permanent `compiler/tests/` guard test greps the same surface for the same regex asserting zero hits modulo the three documented exceptions. SPEC §6.3 "at most one of: heroes, replica items, monsters" uniqueness prose survives verbatim. Chunk-internal: exact regex form + the chunk-time hit enumeration live in the chunk plan. | — |
| `nonsummon-typed-ir` | Atomic retype of `ItempoolItem::NonSummon { name, tier, content: String }` to `NonSummon(NonSummonEntry)` recursive sum (SPEC §3.7 atomic-replacement; every callsite migrated in one commit). Cross-chunk-stable surface: `NonSummonEntry` declared in `compiler/src/ir/`; `SummonClassification` declared in `compiler/src/extractor/replica_item_parser.rs`. Transitional `extract_from_itempool` stub-body returns one `NonSummonEntry::Unclassified(...)` per itempool body per decisions.md 2026-05-13 (paired `Finding` populates `field_path` + `suggestion` per SPEC §5). Emitter migration in `compiler/src/builder/replica_item_emitter.rs` lands in same chunk; today's stub-sentinel protocol (empty-name + None-tier + non-empty-content) retires alongside. New pub enums (`NonSummonEntry` plus leaf sub-enums per the chunk plan's variant set) are input-bytes-determined and ship unmarked + corpus-evidenced per decisions.md 2026-05-13's inverted opt-out semantics (`SummonClassification` is not a `ModIR` field and so is not in the audit's reachability scope). Per-variant evidence-corpus extension obligation handled in same chunk per `audit-harness`'s cross-chunk-contract. Four-mod round-trip bar holds against the typed schema. Chunk-internal: leaf-enum variant set + per-variant round-trip guards live in the chunk-plan sidecar at `features/compiler-invariant-completions/implementation/nonsummon-typed-schema-decisions.md` per decisions.md 2026-05-13. The chunk opens only after the Pre-Foundation precondition in the Dependency graph below (`/plan-author --rewrite` against the legacy chunk plan) lands. The T34 compile-time non-constructability of `NonSummonEntry::Unclassified` from the authoring path is NOT enforced in this chunk — see `nonsummon-typed-authoring` (transitional gap bounded per decisions.md 2026-05-14 "Decompose four feature-shaped chunks"). | — |
| `nonsummon-typed-authoring` | Authoring builders at `compiler/src/authoring/non_summon_entry.rs` per the no-new-author-ergonomics Non-goal's carve-out (the SPEC §3.3 strict-validation site paired with the SPEC §3.2 typed-IR commitment). T34 enforces compile-time non-constructability of `NonSummonEntry::Unclassified` from the authoring path (only the extractor produces this variant). T30.16c enforces authoring-side rejection of dual-Some sidesc combinations the IR permits at extract time. Chunk-internal: exact builder signatures + T34 mechanism (e.g. private constructor, sealed trait, `pub(crate)` scoping) live in the chunk plan. | `nonsummon-typed-ir` |
| `typed-summon-extractor` | Replace `extract_from_itempool` transitional stub with real classifier detecting `hat.egg.<enemy>` + `vase.(add.((replica.<team>.n.<same-Pokemon>...)))` summon shapes; routes matches to `ItempoolItem::Summon(usize)` + populated `ReplicaItem`; routes non-matches to typed `NonSummonEntry` variants (consumes the `nonsummon-typed-ir` typed sum); widens `ReplicaItem.enemy_template` from its current `String` shape to a new pub enum (a recursive sum per decisions.md 2026-05-12 nested-egg shape — Red Orb shape `hat.egg.(wolf.n.Geyser…)` inside `hat.egg.dragon.n.Groudon` round-trips byte-equal; every depth-N nesting structurally encoded; flat sequences excluded); the new pub enum classifies under the opt-out vehicle marker as input-bytes-determined (unmarked + corpus-evidenced); adds typed cross-bucket fields to `Finding` per the bound "Finding typed cross-bucket field shape" decisions-closure row and migrates every `Finding` construction site; classification reads inner payload bytes rather than container-name substrings (the typed-summon-extractor Goal verifier's source-vs-IR adversarial pair lives in the chunk plan); the typed-summon-extractor Goal verifier "no `message.contains` predicate over bucket-label literals survives in the test suite" holds at chunk-land; the four-mod round-trip equality bar holds with the typed classifier producing real summons per the brief verifier's per-mod counts | `nonsummon-typed-ir` |
| `xref-context-check-introduction` | Introduces two new public functions in `compiler/src/xref.rs` — `check_replica_item_in_context` and `check_monster_in_context` — parallel to the existing `check_hero_in_context` and `check_boss_in_context`. Together the four context-checks form a uniform per-entity pattern; every `ModIR::add_*` CRUD op invokes its matching context-check as the sole pre-add validation gate. The private `find_name_category` helper retires fully (zero callers, retirement-protocol guard test per CLAUDE.md "Retiring a public identifier"). The standalone color-uniqueness check in `add_hero` retires alongside `find_name_category`'s invocation there (V019 now emitted only from `check_hero_in_context`). Brief Goal 1's "exactly one finding per defect" property is NOT yet delivered in this chunk — see `xref-narrowing-and-rename` (transitional gap bounded per decisions.md 2026-05-14 "Decompose four feature-shaped chunks"). Chunk-internal: per-CRUD-op retirement procedure, ValidationReport→CompilerError conversion-helper signature, and test-case scaffolding live in the chunk plan. | `typed-summon-extractor` |
| `xref-narrowing-and-rename` | Lands the V020 narrowing predicate at every emission surface (shared helper called from `check_cross_category_names` + every per-entity context-check branch per decisions.md 2026-05-11 "V020 narrowing covers every xref.rs emission site"); per-target X003 routing through `check_hero_in_context` / `check_replica_item_in_context` / `check_monster_in_context`; X003 owner-map literal flip `"legendary"` → `"replica_item"`; `humanize_bucket` helper renders SPEC §6.3 prose at every `buckets.join` site inside `check_duplicate_pokemon_buckets`; rules dispatch on typed `Finding.buckets` / `Finding.includes_boss` rather than `message.contains` scans. CLAUDE.md retirement-protocol guard test for the retired `legendary` identifier across `compiler/src/**/*.rs` asserting zero hits modulo the documented `compiler/src/builder/mod.rs` historical-anchor exception. Delivers the dedupe Goal verifier: a name collision across `{hero, replica_item, monster}` produces exactly one X003 finding regardless of which CRUD add-op the caller invoked; a boss-involving collision produces exactly one V020 finding; a hero color collision produces exactly one V019 finding. | `xref-context-check-introduction` |

## Risks / unknowns

- **`nonsummon-typed-ir` chunk plan format normalization is unfinished.** Bound as the Pre-Foundation precondition in the Dependency graph below — `nonsummon-typed-ir` does not open until `/plan-author --rewrite` against `features/compiler-invariant-completions/implementation/nonsummon-typed-schema.md` produces two co-located chunk plans (`nonsummon-typed-ir.md` + `nonsummon-typed-authoring.md`) that replace the existing file per decisions.md 2026-05-14 "Nonsummon chunk-plan file shape".
- **First run of `audit-harness` may identify unevidenced shipping shapes the brief did not pre-name.** The audit is mechanical; any unevidenced shape and its bound doc-surface grep (`personas/*.md` + `SPEC.md` per decisions.md 2026-05-08) land within `audit-harness`'s own chunk per the class-defined variant-deletion bundle. Mitigation: chunk plan opens with a current-state audit listing the deletion candidates.
- **`harvester-pure-core`'s pre-vs-post SHA-256 divergence check happens entirely within its own PR per decisions.md 2026-05-14 "Harvester SHA ledger consolidation".** Divergence between the pre-refactor SHA (captured at chunk open) and the post-refactor SHA (captured at chunk close) indicates pre-existing latent non-determinism or out-of-scope structural change; the chunk is REJECTED on divergence per decisions.md 2026-05-11 round-1 resolution. The ledger is the chunk's PR-attached test evidence. `harvester-determinism-audit` does NOT carry this one-time refactor-time check — it ships the recurring CI gate against `include_str!`-baked corpus, a separate concern.
- **The PoolReplacement structural in `complete-regenerators` is corpus-evidenced in `working-mods/punpuns.txt` via `((heropool.…))` byte form.** `audit-harness`'s transitive walk passes on it (the IR has a `PoolReplacement` variant the classifier routes); the regenerator chunk delivers the production-path completion (`generate_pool_replacement`) that the merge strip-and-regen dispatches on. Mitigation: `audit-harness` and `complete-regenerators` may land in any order across waves; neither chunk's success depends on the other's having landed.
- **Cross-chunk vehicle-attachment is class-defined per decisions.md 2026-05-13.** The `InternalStateEnum` marker trait lands in `audit-marker`'s chunk; `audit-marker-retrofit` applies the marker to the three existing Layer-2-reachable internal-state enums; every chunk that introduces a new internal-state pub IR enum reachable from `ModIR` writes `impl InternalStateEnum for X {}` near the enum declaration in its own module. The audit's `syn`-source-parse CI test (in `audit-harness`) discovers both the `pub enum X` declarations and the `impl InternalStateEnum for X` impls, so the marker attachment requires no within-Second-wave ordering edge against `audit-marker-retrofit` at CI time — the discovery is post-merge. Source-branch sequencing: writing `impl InternalStateEnum for X {}` requires the trait declaration to be in tree, so a Second-wave chunk's branch that needs to attach the marker rebases past `audit-marker`'s merge before compiling. Under the current Foundation-wave chunk set this sequencing constraint is dormant: `NonSummonEntry` and the `enemy_template` recursive sum are both input-bytes-determined (no marker attachment); `DerivedKind` is `ModIR`-unreachable (helper-only, audit-exempt by Layer 2 reachability and no marker attachment needed). A future Foundation-wave chunk introducing a new `ModIR`-reachable internal-state enum would re-surface the rebase requirement. The audit's CI test fires once `audit-harness` merges, enforcing the property on every subsequent CI run.
- **The `Finding.buckets` migration in `typed-summon-extractor` requires removing every `message.contains(<bucket-label>)` predicate from the test suite.** The typed-summon-extractor Goal verifier explicitly requires "no `message.contains` predicate over bucket-label literals survives in the test suite". Mitigation: chunk plan's pre-implementation gate includes `rg "message.contains" compiler/src/xref.rs compiler/tests/` to enumerate the migration universe (note: enumeration scope must cover both compiler/src/xref.rs's in-source test module AND compiler/tests/ per round-8 reviewer state polish-plateau observation).
- **Transitional T34 gap in `nonsummon-typed-ir`.** The atomic typed-IR retype lands without the SPEC §3.3 strict-authoring complement; T34 (compile-time non-constructability of `NonSummonEntry::Unclassified` from the authoring path) is delivered by `nonsummon-typed-authoring`. Bounded by Second-wave landing per decisions.md 2026-05-14. Mitigation: `nonsummon-typed-authoring` is the immediate downstream sibling whose only dependency is `nonsummon-typed-ir`; both land in the same release cycle.
- **Transitional brief Goal 1 violation in `xref-context-check-introduction`.** The per-entity context-check introduction + `find_name_category` retirement lands without the V020 narrowing predicate that delivers the dedupe property; V020 and X003 both fire on `{hero, replica_item, monster}` subset collisions in the transitional window between this chunk's PR merge and `xref-narrowing-and-rename`'s PR merge. Bounded by Fourth-wave landing per decisions.md 2026-05-14. Mitigation: `xref-narrowing-and-rename` is the immediate downstream sibling whose only dependency is `xref-context-check-introduction`; no Goal-1-asserting test ratchets between the two landings.

## Rollout plan

The compiler ships as a single Rust crate consumed by `compiler/src/main.rs` (CLI) and downstream features. No flag system, no migration window, no telemetry. Each chunk lands as one PR; each PR re-runs the four-mod round-trip baseline (`cargo run --example roundtrip_diag`) and `cargo test`. Failures block the merge. The four corpus mods (`working-mods/{sliceymon,pansaer,punpuns,community}.txt`) are the rollback authority — any chunk whose merge degrades round-trip equality on any of the four reverts.

## Dependency graph

```
Pre-Foundation precondition:
   `/plan-author --rewrite` against `features/compiler-invariant-completions/implementation/nonsummon-typed-schema.md`,
   producing TWO co-located chunk plans (`nonsummon-typed-ir.md` + `nonsummon-typed-authoring.md`) that REPLACE
   the existing `nonsummon-typed-schema.md` per decisions.md 2026-05-14 "Nonsummon chunk-plan file shape".
   Both sub-chunk plans must normalize to this feature's chunk-plan format before nonsummon-typed-ir opens.

Foundation wave (parallel, after precondition):
   audit-marker
   harvester-pure-core
   complete-regenerators
   authorable-new-constructors
   summon-name-rename
   authority-doc-vocab-alignment
   nonsummon-typed-ir (← Pre-Foundation precondition)

Second wave (after Foundation):
   audit-marker-retrofit (← audit-marker)
   harvester-determinism-audit (← harvester-pure-core)
   nonsummon-typed-authoring (← nonsummon-typed-ir)
   typed-summon-extractor (← nonsummon-typed-ir)

Third wave (after Second):
   audit-harness (← audit-marker-retrofit)
   xref-context-check-introduction (← typed-summon-extractor)

Fourth wave (after Third):
   xref-narrowing-and-rename (← xref-context-check-introduction)
```

**Cross-wave rule:** within each wave, chunks land in any order; cross-wave dependencies (the `←` arrows above) are the only ordering constraints. Per decisions.md 2026-05-14 "Decompose four feature-shaped chunks", the audit chain is now three ordered chunks; the harvester chain is two; the typed-IR/dedupe chain is four (with `nonsummon-typed-authoring` as a parallel-to-typed-summon-extractor sibling whose only dependency is `nonsummon-typed-ir`).

**Transitional-correctness windows.** Two chunk pairs ship with explicit transitional gaps bounded by immediate-downstream-sibling-same-release-cycle constraints per decisions.md 2026-05-14:
- `nonsummon-typed-ir` → `nonsummon-typed-authoring`: T34 compile-time non-constructability of `NonSummonEntry::Unclassified` from the authoring path is NOT enforced until `nonsummon-typed-authoring` lands; direct struct/enum construction in test code admits `Unclassified` in the interim.
- `xref-context-check-introduction` → `xref-narrowing-and-rename`: brief Goal 1's "exactly one finding per defect" property is NOT delivered until `xref-narrowing-and-rename` lands; during the transitional window V020 still fires from every per-entity context-check branch alongside X003 on `{hero, replica_item, monster}` subset collisions (two findings on the same defect).

**Contract edges (zero PR-diff).** The following cross-chunk contracts exist as forward-binding obligations distinct from code-dependency edges above. Each chunk that ships the named contract holds it stable for downstream consumers; chunks consuming a contract verify the contract's shape against the bound Decisions-closure rows, not against the contract chunk's source code:

- `audit-marker` → all chunks landing new internal-state pub IR enums reachable from `ModIR`: `InternalStateEnum` opt-out marker trait at `compiler/src/audit_markers.rs` available for impl-attachment per the rustdoc-on-impl discipline.
- `audit-marker-retrofit` → `audit-harness`: the three existing Layer-2-reachable internal-state enums (`Source`, `StructuralType`, `Severity`) carry `impl InternalStateEnum` impls before the harness audits.
- `audit-harness` → all chunks landing new input-bytes-determined IR enums reachable from `ModIR`: every chunk landing new input-bytes-determined variants extends the audit's evidence corpus in the same chunk that lands the enum declaration — the representation of that evidence corpus is chunk-internal to `audit-harness` (the chunk plan picks the data shape); the property bound here is per-variant evidence coverage, not the data-structure name. The audit's CI run gates this on the chunk's own PR. Enumerated downstream consumers today: `nonsummon-typed-ir` (lands `NonSummonEntry` plus leaf sub-enums); `typed-summon-extractor` (lands the `enemy_template` recursive sum).
- `complete-regenerators` ↔ `audit-harness`: `REACHABILITY_CARVE_OUT` list extension for `"DerivedKind"` (the `complete-regenerators`-introduced helper enum that is Layer-2-unreachable from `ModIR`). The obligation transfer is bound by decisions.md 2026-05-13 "`DerivedKind` classification disposition independent of merge order" — whichever chunk merges second carries the extension in its own PR. The audit's CI run on the second-to-merge chunk's PR gates the property.
- `harvester-pure-core` → `audit-harness`: `textmod_compiler::authoring::sprite::SPRITE_REGISTRY` access-path stability for unknown-sprite escape-hatch detection (per "`SPRITE_REGISTRY` access-path stability" row).
- `harvester-pure-core` → future browser/mobile mod-builder app feature: harvester pure-core function signatures (`harvest_face_ids`, `harvest_sprites`) reachable from library code (per "Harvester core invocability and input shape" row).
- `nonsummon-typed-ir` → `typed-summon-extractor`: `NonSummonEntry` typed sum + `SummonClassification::Typed(_)` shape pinned at the symbol level (per the `nonsummon-typed-ir` chunk plan's IR schema block, which descends from the legacy `nonsummon-typed-schema.md` per the Pre-Foundation precondition).
- `nonsummon-typed-ir` → `nonsummon-typed-authoring`: the typed `NonSummonEntry` symbol available for the authoring builders to construct (T34 enforces that `Unclassified` is excluded from the authoring path).
- `typed-summon-extractor` → `xref-context-check-introduction`: `Finding.buckets` + `Finding.includes_boss` typed-field shape (per "Finding typed cross-bucket field shape" row) available before the per-entity context-checks land.
- `xref-context-check-introduction` → `xref-narrowing-and-rename`: the four uniform per-entity context-checks + `ModIR::add_*` CRUD routing through them, ready for the V020 narrowing predicate + X003 routing + vocabulary rename to land on top.

## Out of scope

- No re-prosecution of already-shipped platform invariants, with bounded carve-outs for explicit deferred residuals (per the no-platform-re-prosecution Non-goal).
- No new author-ergonomics surface, with bounded carve-outs (per the no-new-author-ergonomics Non-goal).
- No parser or emitter behavior changes beyond what the four bounded carve-outs require (per the no-parser-emitter-changes Non-goal).
- No drift-class parser/emitter fixes outside the four bounded carve-outs (per the no-drift-class-fixes Non-goal; open PIPELINE_FIDELITY drift classes plus `HiddenModifierType::Skip` placeholder owned by future `features/pipeline-fidelity/` feature).
- No game-balance, roster, or corpus changes (per the no-game-balance Non-goal).
- No expansion of author-content cross-IR semantic checks beyond deduplicating the two existing collision rules (per the no-author-content-rule-expansion Non-goal; the audit chain `audit-marker`/`audit-marker-retrofit`/`audit-harness` is a structural check on IR-shape, not an author-content rule).

---

Per-chunk implementation plans live in [`implementation/`](./implementation/) and are written just before each chunk starts (see `features/README.md`).
