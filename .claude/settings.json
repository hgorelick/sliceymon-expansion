{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r p; case \"$p\" in */plans/*.md|plans/*.md) printf '%s' \"{\\\"hookSpecificOutput\\\": {\\\"hookEventName\\\": \\\"PreToolUse\\\", \\\"additionalContext\\\": \\\"Chunk-implementation checklist (treat the plan as untrusted input):\\\\n\\\\n- Verify the chunk against its cited authority. If the chunk restates a \\\\u00a7F / SPEC / contract-level rule, diff the two \\\\u2014 they must be identical or the chunk strictly narrower. A contradiction means the plan is wrong; fix the higher-level source first, in the same PR.\\\\n- A \\\\u00a7F block is not immune. If two sentences inside the same \\\\u00a7F or chunk name the same canonical set (buckets, fields, enum variants, rule IDs), diff the sentences \\\\u2014 they must be byte-identical. Name the set once and reference it by name afterwards; never re-list its members. When the canonical set is implementation-shaped (e.g. enum variants), let the implementation be the authority and align prose to it; don't promote a prose gloss into a new variant.\\\\n- Every IR variant discriminator must have at least one corpus instance per variant before it ships. Grep working-mods for an instance of each variant. Zero instances for a variant means the variant is a hypothesis masquerading as a model \\\\u2014 delete the variant rather than carry unevidenced cases. A rule (xref, V-rule, authoring gate) authored against an unevidenced variant compounds the defect.\\\\n- Prove your tests catch source-vs-IR divergence, not just IR-vs-IR. If the change is stable under an idempotent roundtrip, equality baselines will pass even when output is silently wrong. Add a test whose input would be interpreted differently if the code reached for a derived / canonical / registry data source instead of the source bytes.\\\\n- Integration assertions over working mods or baselines must encode the reason a rule pair, co-fire, or residual finding is permitted \\\\u2014 not just the pair identity. A predicate that only matches on rule-id equality is an accepting oracle; it green-lights every future regression of that exact shape. State the invariant (e.g. collision includes a boss bucket, collision spans two Pokemon containers) and assert it.\\\\n- Watch for structural smells. A chunk that collapses two paths with different invariants (e.g. permissive extract vs strict authoring) is a spec amendment masquerading as an implementation detail \\\\u2014 stop and raise it. A chunk that pastes the same N-line incantation across many callsites is asking you to encode a mistake N times \\\\u2014 extract a helper or write the single correct line instead.\\\\n\\\\nApplies to every chunk in every plan under plans/.\\\"}}\" ;; esac; }"
          }
        ]
      },
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -n '{hookSpecificOutput:{hookEventName:\"PreToolUse\",additionalContext:\"Evidence rule (non-negotiable, applies to EVERY file in this project — source code, tests, plans, specs, docs, configs, hooks):\\n\\nBefore this edit lands, the change itself — not just claims about it — must be justified by tool output produced IN THIS CONVERSATION. \\\"The code looks right\\\" is not evidence. \\\"I remember the signature\\\" is not evidence. Every edit must be traceable to one of:\\n\\n1. Corpus bytes. For parsers, emitters, IR shape, round-trip logic: quote the working-mods/ lines the change is modeled on. Before changing extractor behavior, grep the corpus for the shape being extracted; before changing emitter behavior, grep the corpus for the shape being emitted. Round-trip invariance against working-mods/*.txt is the correctness bar — a change unbacked by corpus is a guess.\\n2. reference/textmod_guide.md. For format semantics, modifier chain rules, nesting, sticker semantics, vase/hat/replica/cast structure, dice encoding: cite guide section + line. When parser, emitter, and guide disagree, guide wins.\\n3. Existing code state. For rename, move, refactor, callsite update, type change: Read the file(s) being changed AND every callsite at its current state. Do not recall signatures, line numbers, or field types from memory. Grep for every caller before changing a type or signature.\\n4. A failing test or reproduced bug. For bug fixes: the bug must be reproduced (test case, roundtrip diff, xref failure) before the fix is written. \\\"This could break X\\\" without a reproduction is speculation, not a fix.\\n5. A named user decision. Design choices, scope calls, trade-offs the user explicitly made in this conversation or in CLAUDE.md/SPEC.md.\\n\\nForbidden shortcuts:\\n- \\\"verified at implementation start\\\" / \\\"re-grep before authoring\\\" / \\\"check the exact line count\\\" — deferred verification. Run the check NOW; do not defer.\\n- \\\"probably\\\" / \\\"should\\\" / \\\"standard pattern\\\" / \\\"convention is\\\" — guess. Read the file.\\n- \\\"based on the plan\\\" / \\\"the plan says\\\" as the sole basis — plans can be wrong. Re-verify against code and corpus.\\n- Writing code from training-data memory of a library/API — Read the local version; Anthropic SDKs, Rust stdlib, and project APIs all evolve.\\n\\nIf evidence is missing: stop and gather it before editing. If the change is genuinely trivial (typo fix, whitespace, lint-only) say so — triviality is its own evidence.\"}}'"
          }
        ]
      }
    ]
  }
}
